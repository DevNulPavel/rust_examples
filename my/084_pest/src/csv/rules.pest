// Символ | значит оператор или
// ()+ значит повторение как минимум один раз данной последовательности
// Пример: 1231.123
// Но проблема в том, что проходит и такая запись: 123-..13
field = { (ASCII_DIGIT | "." | "-")+ }

// Символ ~ является аналогом and_then
// Пример для записи ниже: 123.2,12312,-324
// Символ звездочка значит, что повторяться может 0 или более раз
record = { field ~ ("," ~ field)* }

// SOI - значит начало файла
// EOI - конец файлика
// Пример:
// 132,123123,-345.3
// 123.1,-345,54456
file = { SOI ~ (record ~ ("\r\n" | "\n"))* ~ EOI }