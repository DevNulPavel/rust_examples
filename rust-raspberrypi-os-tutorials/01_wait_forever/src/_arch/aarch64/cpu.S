
.section ".text._start"

// Экспортируем функцию _start, которая является входом в программу
.global _start

_start:
1:  
    wfe         // Ждем ивентов
    b       1b  // Если произошло событие - переходим снова к метке 1
